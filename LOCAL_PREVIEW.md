# 로컬 미리보기 체크리스트

웹 배포 전에 로컬 화면을 먼저 안정화하기 위한 짧은 절차입니다.

## 1. 데이터 다시 굽기

```bash
python run_pipeline.py
```

이 명령은 연구 산출물을 `data/processed/dashboard`에 다시 쓰고, 현재 공개 허용 목록에
포함된 최소 JSON만 `dashboard/public/data`에 씁니다.

## 2. 대시보드 켜기

```bash
cd dashboard
npm run dev:local
```

브라우저에서 `http://127.0.0.1:5184`를 엽니다.

## 3. 화면에서 먼저 확인할 것

- 상단에서 `확인된 사실`, `계산으로 보는 것`, `아직 모르는 것`이 구분되는가
- 이름 공개 투표소 직접 연결 결과가 위치 미확인 표차 목록보다 먼저 보이는가
- `50% 가정상 여유`가 실제 배부량이나 잔여량처럼 읽히지 않는가
- 오후 1시 급증이 부족 발생 시각이 아니라고 설명하는가
- 재투표·당락 영향·실제 이탈 인원을 단정하는 문구가 없는가

## 4. 자동 smoke test

로컬 서버가 켜진 상태에서:

```bash
cd dashboard
npm run smoke:local
```

통과하면 `dashboard/dashboard-local-smoke.png` 스크린샷이 생성됩니다.

## 현재 로컬 판단

전국 기준선과 송파구 상세 사례를 함께 보여주지만, 실제 원인 판단은 투표소별
배부·이송 기록 확보 전까지 유보합니다. 로컬 검증에서는 "이 화면을 본 사람이 사실과
가정을 구분할 수 있는가"를 우선 확인합니다.
