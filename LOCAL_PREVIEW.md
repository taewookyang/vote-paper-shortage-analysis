# 로컬 미리보기 체크리스트

웹 배포 전에 로컬 화면을 먼저 안정화하기 위한 짧은 절차입니다.

## 1. 데이터 다시 굽기

```bash
python run_pipeline.py
```

이 명령은 `data/processed/dashboard`와 `dashboard/public/data`에 정적 JSON을 다시 씁니다.

## 2. 대시보드 켜기

```bash
cd dashboard
npm run dev:local
```

브라우저에서 `http://127.0.0.1:5184`를 엽니다.

## 3. 화면에서 먼저 확인할 것

- 첫 화면에 `한눈에 읽기`가 보이는가
- `확인된 사실`, `proxy가 설명 못 하는 곳`, `그래서 필요한 자료`가 구분되는가
- 송파구 타일맵이 27개 동으로 보이고, 빈 검은 칸처럼 보이는 요소가 없는가
- `확인된 송파구 부족 투표소` 표에서 `부족 확인`과 `proxy 계산`이 분리되는가
- `기다리다 포기한 비율`은 용지 부족 수가 아니라 `최대 잠재 영향 인원`을 바꾸는 값으로 설명되는가

## 4. 자동 smoke test

로컬 서버가 켜진 상태에서:

```bash
cd dashboard
npm run smoke:local
```

통과하면 `dashboard/dashboard-local-smoke.png` 스크린샷이 생성됩니다.

## 현재 로컬 판단

아직 전국 배포용 결론 페이지가 아니라 송파구 파일럿입니다. 로컬 안정화 단계에서는
"이 화면을 본 사람이 오해 없이 읽을 수 있는가"를 우선 확인합니다.
